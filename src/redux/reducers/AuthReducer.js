const initialState = {
  userLogin: {}
}
export const AuthReducer = (state = initialState, action) => {
  switch (action.type) {
    case value: {
      return {...state}
    }
    default: {
      return {...state}
    }
  }
}